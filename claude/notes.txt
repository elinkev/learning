notes taken from claude certification playlist - https://www.youtube.com/playlist?list=PLFz7SvAnfqLpjPCPJBkUy077BC5ecuE8c

part 1 - what goes into exam
	- domains
	- tiers (opus/sonnet/haiku) - need to know which one to use
	- sdk / api
	- cenario based questions
part 2 - agentic loops 
	- api & sdk communication stack
		- my code - customer placing an order
		- anthropic sdk - library you install, wraps http complexity into simple method calls - waiter taking the order
		- claude api - handles api key auth and routes the requestt to the right model - kitchen ticket system
		- claude servers - run the language model, return structured response object - kitchen coooking the dish
	- messages.create() (5 parameters)- runs on every loop
		- model (required) - opus/sonnet/haiku
		- max_tokens (required) - if set too low, responses can get cut off, usefull to create distinct stop reason
		- system - persistent instructions that shape every response
		- tools - declere what actions claude is allowed to request
		- messages - full conversation history
	- what are tools - its not code, its a description
		- claude never sees your tool implementation it only sees - name, description, input_schema
		- claude decides what to call, your code decides how to execute it
		- when claude decides to run a tool, it responds with the tool_use_id, name, input, your code runs the function, result returned to claude as tool_result
	- every call to messages.create returns response object - 2 critical fields
		- stop_reason - why claude stopped 
			- end_turn - claude is done -> exit loop
			- tool_use - claude needs your code to act
				- this field has 3 important fields - id (unique), name (which tool claude wants to call), input (arguments for the tool)
			- max_tokens - response cut off -> handle carefully
		- content (array of blocks)
			- type: "text" - claude narration or final answer
			- type: "tool_use" - a tool claude wants to call
			* may contain both in single response
	- 4-step agentic loop pattern
		- 1) call messages.create() - send full conversation history and tool definitions. Claude sees everything accumulated so far
		- 2) check stop_reason - if its end_turn -> exit loop & return answer or if its tool_use -> continue to step 3
		- 3) execute all requested tools - find every tool_use block in content array, run each one, collect results
		- 4) append & loop back to step 1 - add claude response as Assistant message. Add all tool results as User message. Go to step 1
	- loop in action
		- 1) Messages: [user: "get weather in Tokyo and Paris"]
		- stop_reason: tool_use - claude requests get_weather('Tokyo'). Your code executes, returns result
		- 2) Messages: [user, assistant(Tokyo call), user(Tokyo result)]
		- stop_reason: tool_use - claude sees Tokyo done, requests  get_weather('Paris')
		- 3) Messages: [user, asst, user(Tokyo), asst(Paris call), user(Paris result)]
		- stop_reason: end_turn - claude has both results, generates final answer, loop finishes
		* claude figures on its on by reading the growing conversation history - it knows what was fetched previously and what is still missing
	- model-driven vs scripted automation
		- scripted example
			- 1) always search
			- 2) always calendar check
			- 3) always payment
			- order is hardcoded, cant addapt if a step returns unexpected data, you are the planner
		- model-driven example:
			- claude reads conversation context each iteration
			- decides what tool to call based on what it knows so far
			- calls one tool if thats all thats needed
			- addapts to unexpected results, exists as soon as the goal is met, claude is the planner
	- stop_reason - deep dive
		- tool_use - claude is NOT done, it needs to call a tool to continue, find all text_use  blocks, execute every one, append results, loop back
		- end_turn - claude is done. it has all information needed and produced final answer, break out of loop and return text content to user
		- max_tokens - response was cut off, claude hit the token limit before finished
	- 3 anti-patterns to avoid
		- text parsing for loop control - checking literal words like 'done' or 'finished' to exit loop, claude might say 'done' in step 1 but theres still a lot of steps, always use stop_reason
		- fixed iteration cap as primary control - primary exit must always be stop_reason == end_turn
		- keyword detection for tool routing - read the structured tool_use blocks in the content array, dont check if claudes text mentions a tools name to decide what to call
	- 7 things to know
		- loop pattern
		- tools are descriptions
		- stop_reason is the only control signal
		- messages array grow every iteration
		- model-driven means claude what to do next (claude is planner)
		- tool results go back as user role messages, wrapped in tool_result block with the matching id
		- use while true with safety counter as fallback, never fixed range as primary exit condition
part 3 - multi agent coordinator
	- why move from single to multi agent system - coordinator and subagents
		- a single agent can hold limited amount of context
		- each agent can be specialized - coding agent, research agent, writer reviewer etc
		- can run multiple agents simultaneously
		- multi agent only when constraints force it, most tasks can be handled with single agentic loop
	- hub-and-spoke architecture
		- hub - coordinator, all communication go throu coordinator
		- coordinator owns error handling, if subagent fails, its coordinators job to decide to retry or degrade gracefully
		- coordinator controls the flow - it decideds what context the subagents get, subagents dont know anything by themselves or can share info between themselves
	- isolated context
		- coordinator sees everything - original user request, all task tool calls, all tool result responses, full conversation with user
		- subagent - only sees its own system prompt that was passed by the coordinator
	- coordinator responsibilities
		- 1) decompose - break user request into subtasks with clear scopes for each agent
		- 2) delegate - issue task tool calls , one per sub agent, with fully specified prompt that the agent needs
		- 3) aggregate - collect all subagent responses and synthesize them into one coherent view
		- 4) evaluate - check quality and completeness - red-deleage if needed
		- 5) respond - deliver the final answer to the user, only after QA passes
	- the task tool (delegation
		- 1) coordinator emits task call - coordinators response includes a tool_use block with type: "task", target agent name, and a fully specified prompt
		- 2) sdk launches fresh claude instance - sdk spawns new claude session using target agents AgentDefinition - system prompt, tools, model settings
		- 3) subagent runs its own agentic loop - executs its own 4-step loop independently, including any tool calls it needs, until it reaches end_turn
		- 4) result returned as tool_result - the subagents final response come back into coordinators message history as a tool_result. Coordinators allowed_tools must include "Task"
	- parallel vs sequential excecution
		- parallel - coordinator returns two or more Task tool calls in single response, sdk launches all subagents simultaneously
		- sequential - coordinator issues one Task call per loop turn, waits for result before continuing, used when next step needs previous steps output
	- dynamic routing & work partitioning
		- dynamic routing - coordinator evaluates the request first, then decides which subagents to invoke
		- work partitioning - each subagent has distinct, non overlapping scope - no two agents work on the same data
	- iterative refinement & structured handoffs
		- iterative refinement (evaluate)- after receiving subagent response, coordinator checks QA, if something is not right - it re-delegates, not necessary to the same agent
		- structured handoffs - each finding from subagent must be tagget with source name, url, date
	- 5 anti-patterns
		- direct subagent-to-subagent communication - subagents never message each other
		- assuming inherited context - subagents start fresh
		- always-on full pipeline - triggering all subagents on every request regardless of complexity (dynamic routing)
		- overly narrow decomposition - splitting tasks so finely that subagents have overlapping scopes
		- raw text handoffs - passing unstructured text between agents with no source attribution (source, url, date)
	- 9 things to know
		- hub-and-spoke - coordinator and subagents
		- isolated context - subagents start fresh
		- responsibilities - decompose -> delegate -> aggregate -> evaluate -> respond
		- task tool - spawns subagents; coordinators allowed_tools must include "Task"
		- parallel execution - multiple task calls
		- sequential execution - one task per loop iteration
		- dynamic routing - only invoke subagents needed for request
		- structured harndoffs - every response from subagents need to be structured
		- iterative refinement - coordinator evaluates results, re-delegates if needed
part 4 - subagent config & context
	- raw api vs claude agent sdk
		- raw api - /messages endpoint, dirrect http call to claude servers, you manage every loop, every retry
		- sdk - python framework, built on top of raw api, gives structured building blocks - agents, tools, sessions
		- api = library, sdk = framework
	- task tool - how subagents get created
		- Task is a special built-in tool in the SDK, when coordinator calls it - sdk creates brand new agent instance
		- to use Task tool, coordinators allowed_tools MUST include "Task"
		- each task invocation creates new isolated subagent with no fresh context or memory of coordinators conversations
		- task description becomes subagents first user message
		- when subagent is finished, it responds back with "tool_result" in the coordinators messages array
	- AgentDefinition - 5 fields needed to spawn subagent
		- 1) name - agent identifier, used for logging and routing references
		- 2) description - what the agent does
		- 3) system_prompt - specialization instructions
		- 4) tools - list of tool definitions the agent can use - does not grant permission
		- 5) allowed_tools - list of tools that the agent can use, must be subset of tools, this is where minimum-tools design principle is enforced
	- minimum-tools design principle
		- wrong approach - every agent gets every available tool
		- correct approach - every agent gets tools only needed for the job
		- reasons why - security, reliability, cost
	- context passing
		- every subagent starts with zero context, coordinator must construct task description
		- what to pass - relevant facts, constraints, exact output format needed, prior subagent results if needed
		- what not to pass - entire conversation history
	- execution patterns - parallel vs sequential
		- sequential - use when task 2 needs tasks 1 output
		- parallel - use when tasks are independent 
		- mode determined by how you await - sequential awaits run one at a time, gather() awaits run concurrently
		- if task share NO data dependencies -> parallel is correct choice
	- fork sessions - same baseline, different paths
		- fork - divergent subagent spawn - spawn multiple subagents from same baseline context, each exploring different direction
		- use cases - brainstorming, a/b testing, hypothesis investigation
		- coordinator collects data from all forks and synthesizes final decision
	- goal-oriented prompt design
		- goal + output format - what u want subagent to product and in which format
		- constraints - what u want subagent to NOT do or access
		- dont specify exact tool sequence - leave the decision to claude
		- dont over-constrain execution - if u need to specify every step - use a script, not an agent
	- 6 anti-patterns
		- forgetting "Task" in allowed_tools - cant spawn subagents
		- giving all tools too all agents
		- dumping entire conversation history into task prompt
		- procedural prompts that specify every step - let claude decide execution path
		- unstructured data handoffs - raw text blobs with no metadata - always include source, date, confidence
		- treating parallel vs sequential as a style preference
	- 8 things to know	
		- Task tool creates isolated subagents - "Task" MUST be in coordinators allowed_tools
		- AgentDefinition has 5 fields - name, description, system_prompt, tools, allowed_tools
		- Minimum-tools principle - each agent only gets tools needed for the job
		- Subagents start black - pass all needed context: facts, constraints, format, prior results
		- sequential = await chain - parallel = asyncio.gather()
		- Fork = multiple subagents from same baseline, each exploring different direction
		- specify goals + output format - never specify exact tool call sequences
		- Structured metadata in all handoffs - source, date, confidence score for every cross-agent data transfer
part 5 - multi-step workflows
	- probabilistic vs deterministic compliance
		- probabilistic - relying on claude's judgement, you write the prompt, claude reads it and usually follows them, llms can be persuaded and edge cases exist
		- deterministic - using code-level checks, your code enforces the rule, claude's reasoning is irrelevant
		- claude is a language model - it can be persuaded. Given convincing context it might skip a step it would normally follow
		- production rule - safety critical constraints MUST be enforced deterministically in code
	- prerequisite gates 
		- gate - a check that blocks execution of tool B until tool A completes successfully 
		- implementation - wrapper function, before the downstream tool executes, the wrapper checks prerequisite state, if not satisfied - raises an exception
		- example - payment gate - payment_tool can only run after validate_card_tool returns success. Payment wrapper checks a flag set by validator - no flag - no payment
		* gates are code-enforced - claude cannot skip them
	- enforcement vs prompt guidance - when to use which
	 	- code enforcements - safety matters, compliance required, financial transactions, irreversible actions
		- prompt guidance - style preferences, tone and format, non-critical workflow ordering, mistakes that can be corrected
		* in other words - if action cannot be undone - use code enforcements
	- structured escalation handoffs
		- when to escalate - confidence drops below threshold, instructions are ambiguous, irreversible action is pending and agent isnt certain it should proceed
		- what to include in the handoff - summary of what was attempted, current state of the task, specific decision the human needs to make
		- format - structured json with 4 fields - action_requested (what human needs to do), context (what happened so far), options (available choices), recommended_action (what agents thinks is best)
		* escalation can never be vague, never pass "i need help" or "i got confused" for human review, always include actionable decision context
	- multi-concern decomposition example
		- single complex request "is this transaction safe?" 
		- coordinator splits this to 3 agents - fraud_check_agent, compliance_agent, risk_score_agent
		- each concern is isolated, each agent is specialized, each agent run parallel (unless they need each others outputs)
	- hooks in the agent sdk
		- hooks fire automatically around every tool call in the agentic loop
		- PreToolUse hook - fires before execution, usecases - block by raising exception, modify input before execution, log for auditing
		- PostToolUse hook - fires after execution, usecases - validate output, transform result before claude sees it, record the outcome for auditing
		- common use cases - audit logging, rate limiting (block over-called tools), output sanitization (strip sensitive data), schema validation (reject malformed input)
	- hooks code pattern
		- return None - allow the tool through unchanged, tool executes normally
		- raise Exception - block the call, tool will NOT execute, claude receives the error
		- return modified dict - override the input before execution, tool runs with your modified params
		* memorize these 
	- 5 anti-patterns
		- "dont run payment without validation" as prompt, trusting claude will follow it. Correct way is to use code gate that blocks payment_tool, until validate_card_tool passes
		- Claude calls tools out of oder - it thinks skipping a step is efficient. Correct way is to enforce order with prerequisite gate checks
		- escalating with "i need help" - human has no context to act. Correct way is using structured handoff - action_requested, context, options, recommended_action
		- registering hooks but ignoring their return values. Always propagate hook decisions - if a hook block, the tool must not run
		- decomposing depended concerns in parallel - agents that share state or need each others output. Only parallelize truly independent investigations
	- 7 thinks to know
		- probabilistic = claudes judgement (prompts), deterministic = your code (enforcement)
		- safety-critical constraints MUST be code-enforced
		- prerequisite gates block downstream tools until prerequisites succeed
		- irreversibility test - if a mistake cannot be undone -> code enforcement is required
		- structured escalation handoffs - always include action_requested, context, options, recommended_action
		- PreToolUse can block, modify, allow. PostToolUse can validate, transform, record
		- Multi-concert decomposition - parallelize independent concerns only, if concern B need A's output - use sequential
part - 6 
	- domain 1 - task 1.5 - sdk hooks & data normalization
		- transform messy tool data and enforce business policies with code
	- five tools five different formats
		- order system - "created_at": "1710633600" (unix timestamp)
		- customer system - "signup_date": "2025-04-01T10:00:00Z" (iso format)
		- shipping api - "ship_date": "03/17/2025" (US date format)
		- legacy database - "status": "200" (numeric status code)
		- modern api - "status": "active" (string status label)
		* claude has to interpret these values itself and usually it gets it right, but to enforce it you need to use hooks
	- what is data normalization
		- normalization means converting different formats into one format before claude sees them
	- why not just tell claude to use one format
		- prompt instructions are probabilistic, not deterministic, even 1% failure can cause problems
		- hook run after every tool call, works every time
	- transforming data after tool execution
		- claude calls get_order
		- tool returns raw data
		- PostToolUse converts unix timestamp into iso format, maps 200 status to "active"
		- claude receives clean result
	- transforming data before tool execution
		- claude calls process_refund(amount=$750)
		- PreToolUse hook intercept - it checks if amount > $500
		- blocks the tool execution - redirects to escalate_to_manager with full context
		- claude tells customer "escalated to a manager"
	- hooks vs prompts
		- hooks - deterministic
		- prompts - probabilistic
		- "guarantee" or "ensure" - use hooks, "encourage" or "prefer" - use prompts
		- examples when to use hooks - financial limits, data format requirements, compliance polices, acl
	- 5 anti-patterns
		- relying on prompts for data consistency - claude misinterprets unix timestamps, use hooks
		- blocking calls with no explanation - claude retries endlessly, always return clear message and alternative action
		- silently modifying data in hooks - debugging becomes impossible, always log all transformations and keep originals
		- using hooks for soft preferences - adds latency, reserve them for hard rules only
		- no redirect on blocked calls - claude will get stuck, always provide alternative - escalation, different tool, human handoff
	- 7 things to know
		- PostToolUse hooks normalize data into consistent formats
		- PreToolUse hooks block policy-violating calls 
		- hooks are deterministic, prompts are probabilistic (1-5% failure)
		- always explain why call was blocked
		- normalization targets - unix to iso, numeric to string statuses, regional to standard dates
		- keep hooks fast - they run on every tool call, no network calls
		- "guarantee compliance" answer is always hooks, not prompts
part - 7
	- domain 1 - task 1.6 - two fundamental approaches
		- prompt chaining - fixed sequential pipeline, each steps output feeds into the next one, used when steps are known upfront
		- dynamic decomposition - subtasks are generated depending on what is discovered on each step, used when scope is open-ended, cant predict full plan upfront
	- prompt chaining
		- fixed linear pipeline - steps designed upfront, step 1 output -> step 2 input -> step 3 input etc
		- best for structured, repeatable tasks - code review, data extraction, report generation
		- key advantage: focused context windows - each step sees only what it needs
		- example usage:
			- step 1 - multi file analysis (parallel) 
			- step 2 - integration (sequential) 
			- step 3 - summary 
	- dynamic decomposition
		- plan emerges from discoveries - you cannot write step 3 until u see step 1/2 results
		- best for open-ended investigation - "add tests to legacy codebase", "audit for vulnerabilities" - u know the goal but not the path
		- trade-off: higher cost, higher adaptability
		- example usage: 
			- phase 1 - map the structure (discovers: 12 modules, 4 have tests, 8 dont)
			- phase 2 - prioritise (of the 8 tests, which are critical? discovers: 3 critical modules - auth, payment, user-data) 
			- phase 3 - deep investigation (trace all code paths in the auth module. discovers: 15 code paths, 3 with complex branching)
			- phase 4 - generate tests (writes tests for all 15 paths)
			* this is impossible to plan because targets only emerge on phase 1-3
	- choosing the right approach
		- task structure - predictable, repeatable (prompt chaining, open-ended), expiratory (dynamic decomposition)
		- example - code review, data extraction (prompt chaining), "add tests to legacy code" (dynamic decomposition)
		- step dependencies - linear - output feeds next (prompt chaining), conditional - depends on findings (dynamic decomposition)
		- planning - full plan known (prompt chaining), plan emerges during execution (dynamic decomposition)
		- parallelism - often possible within steps (prompt chaining), less predictable - context-depended (dynamic decomposition)
		- cost - lower - fixed step count (prompt chaining), higher - steps may multiple (dynamic decomposition)
	- attention dilution - "lost in the middle"
		- LLMs process the beginning and end of long context more reliably than the middle part
		- impact on multi-file reviews - never send all files in single prompt
		- solution: per-pass decomposition - one file per prompt, then a separate integration pass
	- 5 anti-patterns
		- all files, one prompt - attention dilution trap, always decompose into profile passes
		- using dynamic decomposition for predictable tasks
		- using chaining for exploratory tasks
		- overly narrow subtask definitions - coordinator tasked with researching "create industries" decomposes it only into visual arts subtasks, something like fils or music is missed. Subtasks must cover the full scope
		- no cross-file integration pass - always add a cross-cutting integration pass
	- 6 things to know
		- prompt chaining - fixed sequential pipeline, use for predictable repeatable tasks
		- dynamic decomposition - adaptive plan, use for open ended investigation where scope is unknown
		- split large reviews into per-file passes plus a separate cross-file integration pass
		- attention dilution - LLMs pay less attention for content thats not in beginning or end of context
		- adaptive plans add, remove, reorder steps based on findings
		- chaining is cheaper, dynamic decomposition costs more
part - 8
	- domain 1 - task 1.7 - session
		- session - named, persistent record of conversation that you can return to later, all tool results are saved
	- why session matters
		- day 1 - analyze the codebase, claude builds mental model, identifies critical paths
		- day 2 - write tests based on what was found, claude already knows critical paths so no need to re-analyze again
		- day 3 - fix the bugs that the tests revealed, claude prior context is there, no need to re-explain everything again
	- named session flags
		- --session <name> (claude --session auth-refactor)
		- --resume (claude --resume auth-refactor)
	- when to resume and when to start fresh
		- resume - if the code didnt change, and still doing analysis 
		- resume with caution - if code changed between sessions
		- start fresh - context window mostly exhausted, or exploring new approach
	- stale context problem
		- when u resume a session, old tool results are still there, but files may have changed
		- solution 1 - after resuming the session, tell claude what was changed, works best if only few files were changed
		- solution 2 - start fresh with injected summary, ask claude to confirm or revise those findings on current codebase
	- fork_session - branch at decision point
		- fork_session - makes a branch of current session
		- can be used in parallel, useful to find multiple solutions for a problem then choose the best approach
		- each fork gets complete full history copy 
		- changes in fork A have no effect for fork B
		- forks run in parallel
		- original session remains unchanged, forking is non-destructive
	- when to use fork_session
		- comparing two refactoring strategies before committing
		- testing different testing approaches - unit vs integration focused
		- exploring alternative architectures after shared analysis without losing progress
		- never fork for simple linear tasks
	- session decision framework
		- if the code hasn't changed - resume
		- if the code has changed but context window is not exhausted - resume with caution
		- if the code has changed but context window is exhausted - start fresh
	- 5 anti-patterns
		- resuming code when files were heavily modified - you should start fresh or tell claude explicitly what changed
		- starting fresh when context is still valid - resume instead
		- never using named sessions - you lose all progress when terminal is closed
		- forking for simple linear tasks - only use forks when comparing alternatives in parallel
		- resuming with exhausted context - start fresh with injected concise summary of key findings
	- 7 things to know
		- --resume - resumes named session with full conversation history
		- stale context is biggest risk of resumption
		- mitigation 1: inform the resumed session about specific changes
		- mitigation 2: start a fresh session and inject a structured summary of prior key findings
		- fork_session - creates session branches from shared analysis baseline, use this to compare strategies in parallel
		- start fresh when code changed significancy, context is exhausted, or taking new direction
		- session management is all about preserving valuable analysis while avoiding stale context
part - 9
	- domain 2 - tool design & mcp integration
		- domain 1 is about how agents think and orchestrate
		- domain 2 is about how agents act - the tools they use
	- domain 2 - five tasks
		- task 2.1 - tool interfaces
		- task 2.2 - structured error responses
		- task 2.3 - tool distribution
		- task 2.4 - mcp servers
		- task 2.5 - built-in tools
	- task 2.1 - descriptions are everything
		- tools are descriptions, claude does not read its implementation
	- the misrouting problem
		- if the description of the tool is vague, claude has a chance to not pick it
		- example: get_customer ("retrieves customer info") and lookup_order ("retrieves order details"), prompt - "check my order #1234", claude picks get_customer
	- five elements of a good description
		- purpose - what does this tool do
		- input format - what should the input look like, include examples
		- output - what comes back
		- when to use - in what situation should claude use this tool
		- boundary - when should claude not use this tool 
	- bad vs good description
		- bad - name: analyze_content, desc: analyzes content, input: url: string
		- good - name: extract_web_result, desc: extracts structured data from web pages. Input: valid HTTPS url, Output: title, text, date. input: url: string
		- good description includes detailed explanation, examples, and where to not use this till
	- split generic into specific
		- analyze_document - could mean extract data, summarize, verify
		- it would be better to split such tool and use specific ones
		- extract_data_points, summarize_content, verify_claim_against_source
	- the system prompt keyword trap
		- system prompt "always analyze customer requests carefully"
		- two tools available analyze_content (analyzes content from url), lookup_order (retrieves order details by order id)
		- user prompt "whats the status of oder #123?"
		- claude picks analyze_content tool because of the word "analyze" in the system prompt
		- the fix - rename analyze_content to extract_web_content
		* system prompt names should not match tool names, it may cause claude to pick this tool 
	- tool naming strategies
		- action + object - extract_data_points
		- verb + scope - search_web_articles
		- domain-prefixed - billing_lookup_invoice
		* avoid generic verbs like process, handle, do
	- 5 anti-patterns
		- minimal descriptions - tool descriptions should include purpose, inputs, outputs, when to use
		- overlapping descriptions - each tool must have unique description, otherwise claude can pick any of such tools
		- generic tool names
		- system prompt keyword collisions - tool names should not match system prompt words
		- missing boundary conditions - state when not to use the tool, example - not for PDFs
	- 6 things to know
		- tool description is the primary mechanism how claude chooses the tool
		- five primary elements for tool description - purpose, input format, output, when to use, when not to use
		- split generic tools to specific tools
		- renaming ambiguous tools - dont chose generic names
		- system prompt keywords can make claude pick tools just by keyword matches
		- when exam shows misrouting problem - improve the description first
part - 10
	- domain 2 - task 2.2 - structured error responses for mcp tool
		- when a tool fails, it should return a structured response, not just text
		- example - "errorCategory": "transient", "isRetryable": true, "suggestion": "retry in 2 seconds"
	- the isError flag
		- this is a signal layer
		- when this flag is returned as true, claude know that its an error report, not success data
		- "type": "tool_result", "tool_use_id": "tool_891", "is_error": true, "content": "..."
		- without a structured body, claude guesses the recovery action
		- structured error body is decision layer
	- 4 error categories
		- transient - timeout, service down, agent waits and retries
		- validation - invalid input, agent fix the input and retry
		- business - policy volation, agent explains to user why it wont execute this tool
		- permision - unautharized access, escalates to user
	- 5 structured error fields
		- errorCategory - names failure type (transient, validation, business, permission)
		- isRetryable - true/false
		- message - technical detail for logging, not shown to user
		- customerMessage - what to tell the user, business errors only - must be polished and user friendly
		- suggestion - next action for the agent: retry, escalate to human, ask for correct input
	- access faiilures vs valid empty results
		- search_orders("1234) - tool call
		- isError: true, errorCategory: "transient", message: "DB connection timeout" - claude retries the call
		- isError: false, orders: [], total: 0 - claude tells user "you have no orders"
	- local recovery in subagents
		- local recovery - each subagent tries to handle error itself, if all attempts fail - it returns a structured error response to the coordinator
		- subagent should - retry locally, report with results and alternatives before escalating
		- subagent calls search_web("something") -> gets timeout -> retries locally 1 time -> if sucess returns result to coordinator, if not escalates with context
	- 5 anti-patterns 
		- generic errors - include errorCategory, isRetryable, suggestion
		- returning empty results for failures - an empty array tells the user that nothing exists when in reality it was timeout error
		- retrying business rule violations - set business rule violations as isRetryable: false
		- propagate all errors to the coordinator - handle transient errors locally in subagents
		- no partial results in error reports - always include what was attempted and alternatives exist
	- 6 things to know
		- 4 error categories - transient, validation, business, permission
		- 5 structure error fields - errorCategory, isRetryable, message, customerMessage, suggestion
		- access failures vs empty results
		- local recovery for subagent transient errors
		- isRetryable: false on business violations prevent wasted max_tokens
		- escalation reports - always include partial results and alternatives in escalation reports
part - 11
	- domain 2 - task 2.3 - tool distribution
	- too many tools problem
		- giving too many tools to agent degrades tol selection reliability
		- 4-5 tools is the optimal amount of tools per agent
	- scoped tool access
		- each agent should only get tools that it needs
		- instead of assigning a lot of tools to one subagent, make more specific subagents
	- cross-role tools
		- sometimes subagent needs access to a tool from another domain
		- instead of calling coordinator, making coordinator spawn another agent, then returning result to original subagent, create scoped cross-role tool
		- "name": "verify_fact", "description": ()"quick verification of simple facts: dates, names, statistics", "only use for simple facts", "for complex research return control to coordinator")
		- simple fact checking is handled locally by agent
	- 3 tool_choices modules
		- auto - claude may call a tool or return plain text, claude decides what fits best
		- any - claude must always call some tool, used when structured output is needed
		- forced - claude must call this specific named tool, used in ordered pipelines
	- forced tool selection in ordered pipelines
		- step 1 - forced tool to extract metadata
		- step 2 - auto tool, claude decides what to do with the extracted metadata 
	- constrained alternatives
		- fetch_url - fetches any url from internet
		- load_document - fetches document from approved domain only
		* same concept but narrower scope
	- 5 anti-patterns
		- every agent gets all tools - maximizes decision complexity
		- 18 tools per agent - reliability drops when agent has more than 4-5 tools	
		- no cross-role tools at all - forces expensive roundtrip with the coordinator for simple operations
		- auto for output - using "auto" when structured output is required, claude may return text. Use "any" to guarantee a tool call
		- generic tools - broad tools like fetch_url, replace them with alternatives with limited scope
	- 6 things to know
		- too many tools degrade reliability, aim for 4-5 tools per subagent
		- scoped access - each subagent only gets the tools they need for their role
		- cross-role tools are the exception, they are for high-frequence operations that would otherwise require roundrips with coordinator
		- tool_choice - auto (claude decides), any (must call any tool), forced (must call specific tool)
		- forced tool selection ensures a specific tool runs first, used for ordered pipelines
		- replace generic tools with constrained alternatives that limit scope (fetch_url vs load_document)
part - 12
	- domain 2 - task 2.4 - mcp server integration
	- what is mcp
		- mcp - model context protocol
		- lets claude connect to external services through a uniform interface
		- mcp standardizes the interface
		- provides tools (callable functions), resources (read-only catalogs), prompts (templates)
		- claude discovers all tools simultaneously on connection
	- mcp tool vs regular tool
		- regular tools defined by regular json schemas in your code and executed in your code
		- mcp server tools are defined and executed at the server
		- when claude connects to mcp server, all tools from that mcp are available at once
	- project-level vs user-level config (mcp configuration for team or personal)
		- project level config lives at project root (.mcp.json)
		- this file is commited and shared
		- user level lives in home directory (~/.claude.json)
	- mcp server config environment variable expansion
		- tokens/keys are never hardcoded in json files
		- ${GITHUB_TOKEN} - tells to take the token from developers shell environment
	- mcp resources (catalogs)
		- mcps can expose resources - issue_catagories
		- these catalogs give agents visibility for whats availble before its start acting
		- example whats expiosed - issue summaries, database schemas, documentation outlines, report types
	- community servers vs custom servers
		- community servers cover standard integrations - jira/github/slack
		- custom servers - built when its unique workflow and no community option coverts it - deployment pipeline, crm connector, legacy system integration
		- use community servers first, only build custom servers when no other options
	- enhancing mcp tool descriptions
		- community servers sometimes have weak descriptions
		- you can add tool preference guidance to CLAUDE.md
		- example - MCP - codebase_search (search codebase)
		- example - Built-in tool - grep (a powerful search tool built on ripgrep, supports full regex syntax)
		- example - claude picks grep because it has better description
		- fix - add "when searching for code patterns, prefer codebase_search mcp tool over grep" to CLAUDE.md
	- 5 anti-patterns
		- hardcoded secrets in .mcp.json - use ${token}
		- buildint custom servers for standard integrations - use community servers
		- no resources for content catalogs - agents will keep repeatedly explore tool calls to discover whats available. Expose issue sumaries, schemas, categories as resources
		- shared config at user level - for team based configs they need to be in project root, not ~/.claude.json 
		- minimal mcp tool description - fix the description by modifying CLAUDE.md, not the mcp itself
	- 6 things to know
		- shared vs user level configs - .mcp.json - commited, ~/.claude.json - user stored
		- ${token} - keep actual keys out of config files
		- claude discovers all mcp tools simultaneously at connection
		- mcp resources are read-only catalogs - issue summaries, database schemas, documentation outlines
		- build custom mcp servers when no community ones exist
		- enhance mcp descriptions by modifying CLAUDE.md, not mcp directly
part 13
	- domain 2 - task 2.5 - 6 built in tools
		- grep - search file contents
		- glob - find files by name / path pattern
		- read - load full file contents
		- write - create or completely overwrite a file
		- edit - targeted text replacement in existing file
		- bash - run shell commands
	- glob vs grep
		- glob - searches file names/paths - pattern **/*.test.php
		- grep searches file contents - pattern some_function()
	- two phase search strategy - grep then read
		- phase 1 - grep - find entry points (finds multiple files containing your phrase)
		- phase 2 - read - understand the found files
	- edit vs write
		- edit - makes a targeted text replacement, you provide a string and it changes the other string, used for replacing parcial parts
		- write - replaces entire file with new content, used to create new files or complete rewrites
		* when using edit, the haystack string must be unique, if theres multiple choices - provide additional content until its unique
	- read + write
		- when edit failes (cant find unique haystack) - use read + write
		- read the entire file
		- modify the edit mentally
		- write output the modified version
		* exam tests whats the fallback when edit fails
	- incremental exploration
		- dont read entire codebase
		- grep for entry point
		- read the route file
		- read the specific handler
		- read the billing service
		- in other words - only read stuff that matters, this manages the context window efficiently, this is called incremental exploration
	- 5 anti-patterns
		- reading all files upfront - this fills context window with irrelevent files, start with grep to find entry points, then read selectivly
		- using grep to find file names - use glob
		- using write to modify one line - use edit for small changes
		- using edit on non-unique match - include more surrounding context or fallback to read + write strategy
		- using bash for file search - built in grep and glob are more optimal
	- 6 things to know
		- grep searches file contents, glob searches file names and paths
		- search strategy - grep first, then read 
		- edit for targeted changes require unique match, write for new files or complete rewrides
		- edit fails on non-unique match - use fallback - read + write
		- incremental exploration - follow imports and functions calls step by step - dont read entire codebase
		- bash is for build, test, install, git - not for filesearch, use built in grep and glob tools
part 14
	- domain 3 - task 3.1 - claude.md hierarchy
		- claude.md gives claude specific instructions
		- every time claude strarts a session it reads claude.md file
		- it exists at three level scopes - subdirectory, project, user level
	- three level hierarchy
		- (lowest tier) ~/.claude/CLAUDE.md - user level, not shared, applies to all projects
		- (mid tier) .claude/CLAUDE.md or ./CLAUDE.md - project level, used for team standardizes
		- (highest tier) packages/billing/CLAUDE.md - most specific, overrides eevrything below it, shared via git
	- the @import syntax for large CLAUDE.md files
		- the root CLAUDE.md uses @import (@import ./standards/testing.md)
		- keeps the root file at ~50 lines
		- this is known as a modular organization pattern
	- auto-loaded rule files
		- .claude/rules directory 
		- files in this directory are automatically loaded as additional instructions - no @import needed
		- support yaml format
	- splitting a monolithic CLAUDE.md
		- split large files into either imports or rules - testing, deployment, code-style etc
		- root file should be small
	- diagnosing configuration with /memory command
		- /memory - check what configuration files are loaded in current session
		- example - shows user-level, project-level, rules
	- 5 anti-patterns
		- massive monolithic claude.md file - split it with imports or rules
		- team conventions at user level config - other team members wont get them, put them at project-level
		- personal prefs at project-level - personal preferences belong in ~/.claude/CLAUDE.md - not commited
		- no modular organization - use imports or rules
		- not checking /memory when claude ignores rules - verify that claude sees the config
	- 7 things to know
		- three config levels - user, project, subdirectory
		- priority tiers - subdirectory > project > user
		- add @import external md files to keep claude.md small and readable 
		- .claude/rules/ - directory holds rules and no imports needed, this is loaded automatically
		- split monolithic claude.md files to small file with imports/rules
		- use /memory to verify that claude is loading your rules
		- team member missing conventions - check if config is at user-level instead of project-level
part 15
	- domain 3 - task 3.2 - slash commands & skills
		- claude can be extended with custom slash commands
		- .claude/commands/ - markdown files that any user can run
		- .claude/skills/ SKILLS.md file - configures permissions, isolation, input hints
		- command - keyboard shortcut
		- skills - plugin 
	- custom slash commands
		- can be personal or team commands - same scoping as claude.md
		- .claude/commands - team
		- ~/.claude/commands - personal
		- .claude/commands/review.md - can use /review - claude reads the md file and executes whats written there
	- skills with yaml formatter
		- more poverfull than commands
		- context: fork - run isolated sub-agent
		- allowed-tools - which tools to give to the agent (grep, glob)
		- argument-hint - ask the user for input (which file path)
		- context: fork - doesnt polute main conversation, only receives the result - use when exploring, analysis, codebase scanning, brainstorming
		- allowed-tools - least applied principle - if skill is only for analysis, no need to have write/edit tools, restrict it to only read/grep/glob
	- skills vs claude.md
		- claude.md - always loaded automatically, used for universal standard (never commit secrets to git, write tests before implementation)
		- skills - on-demand, used when explicitly invokde (review/analyze/deploy-check)
	- 5 anti-patterns
		- team commands at user level - know personal vs project level
		- skills without fork context - verbose output will fill the main conversation, use isolated sub-agent with fork
		- skills for full tool access for read only tasks - use allowed-tools to restrict/protect from executing unwanted tools
		- universal sdandard as skill - if something needs to be run always - use claude.md, if something needs to be run sometimes - use skills 
		- user skills with same name as project skills - project level will override user level - use different name to avoid this collision
	- 6 things to know
		- commands - user vs project scope 
		- skills support with yaml formatter, context:fork, allowed-tools, argument-hint
		- content: fork - isolates skill in sub-agent
		- allowed-tools - restricts tool access
		- skills are on-demand, claude.md always loaded - universal standards stay in claude.md, tasks worflows go in skills
		- personal skill variants must have different name to avoid overriding team skills
part 16
	- domain 3 - task 3.3 - path specific rules
	- one-size-fits-all rules
		- scenario - large code base, a lot of rules exist in claude.md (react conventions, api validation rules, database patternts etc), editing button.tsx - all rules still get loaded
		- this creates 3 problems: wasted tokens by consuming irrelevant tools, irrelevant guidance (may apply database tool when using react), non-deterministic - claude guesses what to use
	- ,claude/rules/ with path formatter
		- at the top of the md fule, add yaml path for files that should use this rule
		- this makes the rule only load when specific file matches the glob path
		- example - paths: - "src/api/**/*
		- this is key advantage of having rules instead of adding eveyrthing to claude.md
	- glob pattern routing
		- path: ["**/*.test*] - all test files any directory any extensions
		- path: ["src/api/**/*] - all files in the api directory
		- path: ["**/*.tsx"] - all react components
		- path: ["terraform/**/*] - any terraform files in report
		- paths: - "src/api/**/*" - "src/middleware/**/* - multi pattern
	- paths vs sudirectory claudem.md files
		- .claude.md applies to all files inside that directory and its subdirectories
		- path-specific rule applies to any file regardless of where it lives in the tree
		- claude.md - location-bound, directory scope
		- rules - pattern-bound, cross directory
	- 4 anti-patterns
		- using subdirectory claude.md for cross-cutting concerns - its inefficient to put claude.md files in every directory - use path specific rules
		- no paths field in rules file - without path field rules always loaded, use path to scope the rule
		- overly broad glob patterns - be specifc
		- relying on claude to infer which section applies - use paths
	- 5 things to know
		- .claude/rules/ files with a path enable conditional convention loading
		- glob patterns control when rules load 
		- path-specific rules beat subdirectory claude.md for cross-cutting concerns (tests spread everywhere, would need to create a lot of claude.md files)
		- token saving - only load relevant conventions by narrowing path scope
		- maintaining conventions for files spread across many directories - use path-specific rules, not subdirectory claude.md files
part 17
	- domain 3 - task 3.4 - when to use plan mode vs direct execution
		- direct execution - claude reads your request and immediately executes changes
		- plan mode - claude explores codebase, proposes a plan, waits for approval
	- 4 signals for plan mode
		- multiple valid approaches - explore the options before committing
		- large scope - mistakes mid-way may require costly undo work, plan before committing
		- architectural decisions - choosing between integrations
		- risk of expensive rework - picking the wrong approach may cause undoing
	- microservices restructuring
		- without plan mode - claude would start splitting files immediately, discover dependency problems halfway through, costly rework and lost progress
		- with plan mode - claude first maps the code, maps dependencies, proposes a plan, you review and adjust, then commit
		- key difference is human review 
	- 4 signals for direct execution
		- well-defined change - add input validation to this function
		- single file or small scope - bug fix with clear stack trace
		- no architectural decision - add a date validation conditional
		- known approach - convert this callback to async
		- if youcan describe the change in one sentence and theres only one way to do it - use direct execution
	- the explore subagent in plan mode
		- the explore subagent reads files in isolation, keeps verbose output out of the main conversation
		- without this subagent it would consume context before implementation even starts
	- combining plan mode and direct execution
		- first use plan mode to create a plan
		- then execute by planned and reviewed steps
	- 5 anti-patterns
		- using direct execution on architectural change - use plan mode
		- using plan mode on a single file bufxi - unnecessary overahead, scope is already clear
		- starting over when complexity is found - start with plan mode when complexity is suspected
		- plan mode without reviewing the plan - the checkpoint is the whole point of plan mode, review it before committing
		- skipoping explore subagent - main context fills with verbose output from discovery, use explore agent to isolate investigation output
	- 6 things to know
		- plan mode - architectural changes, multi-files changes, multiple approaches, migrations
		- direct execution - single file changes, simple bug fixes, known approaches
		- explore subagent runs in isolation when investigating, only clean output returns back to main context
		- combine both modes, first plan, then review, then execute
		- when exam describes monolith-to-microservice or a large migration - use plan mode, adding a simple validation - direct execution
		- plan modes value is in the human review checkpoint - always review the plan and adjust if needed
part 18
	- domain 3 - task 3.5 - iterative refinement
	- what is iterative refinement
		- scenario - "convert dates to iso, normalize names, extract first email"
		- claude interprets each instruction differently
		- slightly different results each time
		- scenario - "show 3 inputs labeled 'this is what i want' and 3 labels 'this is not what i want' 
		- claude remembers the transformation rule from patterns
		- consistent results on each run
	- concrete input/output examples
		- vague prompt - "extract and normalize contact information from documents"
		- this is vague because claude guesses things like - format, how to handle missing fields
		- concrete prompt - "input: "Contact John Smith at something@example.com, output: {"name": John Smith, "email": something@example.com}"
		- this prompt gives exact format that you want
	- test-driven iteration
		- write test first
		- ask claude to implementation
		- share failed tests
		- ask claude to fix
		- this gives claude specific feedback what to implement
	- the interview pattern
		- before implementing a solution in unknown codebase, you ask claude to interview you
	- sequential vs parallel fixes
		- interacting issue - fix together
		- example for interacting issue - change function to instead of accepting string value, it now accepts null|string, need to fix all places that call this function
		- independent issue - fix sequentially
		- example for independent issue - error handling, unused imports
		* ask yourself whether fixing issue A changes the behavior of issue B, if yes - fix together, if no - fix sequentially
	- 5 anti-patterns
		- only using descriptions - provide 2-3 concrete input/output examples
		- implementing without tests - write tests first, then iterate using failures
		- fixing interacting issues one at the time - address interacting issues in one pass
		- skipping interview pattern in unknown codebases - ask claude to interview you
		- vague error reports - share tests failures with expected results 
	- 5 things to know
		- concrete input/output examples are more effective than prose descriptions
		- test-driven iteration - write tests first, run, then iterate failures until pass
		- interview pattern - tell claude ask questions before committing in unfamiliar domains
		- interacting issues - must be fixed in one message
		- specific tests cases - most efficient way to handle edge cases
part 19
	- domain 3 - task 3.6 -  ci/cd integration
	- what is ci/cd
		- automated pipeline that runs every time code changes
		- claude runs as automated worker in your pipeline, can review pull requests
		- it can be done with the -p flag
	- the -p flag (--print)
		- this makes claude run non interactively
		- it processes the prompt, outputs the result to stdout and exist
		- example - claude -p "analyze this pr for security issues" - this will run, print output, exit
	- --output-format json
		- tells claude to return structured json
		- you can tell which schema to use by adding --json-schema
		- example - claude -p --output-format json \ --json-schema '{"type":"object", "properties" ...}' \ "review changed files"
	- CLAUDE.md as CI context
		- when claude runs in ci, it reads projects claude.md file just as interactive session
		- can add review standards like handle error handling as high 
	- session context isolation
		- the session that wrote the code is less effective to review the code than a new session
		- ci based reviews start from clean session, far more likely to catch issues
	- avoiding duplicate findings
		- include prior findings, only report new findings
	- 6 anti-patterns
		- running claude -p in ci/cd - without the -p flag it runs as interactive session, doesnt exit
		- reviewing code in same session where the code was written - claude will be biased, likely wont catch issues
		- no prior findings in re-reviews - include prior findings, report only new issues
		- no CLAUDE.md for CI context - document review criteria in CLAUDE.md
		- text output in CI - use --output-format json
		- not providing existing tests while generating new ones - include existing test files in context
	- 6 things to know
		- -p flag runs claude non interactively
		- --output-format json - produces structured json response
		- claude.md provide consistent context for both interactive and non interactive sessions
		- session isolation - always review in new session
		- include prior findings to re-reviews to prevent duplicates
		- include existing tests when generating new ones to avoid duplicate coverage
part 20
	- domain 4 - task 4.1 - prompt design & explicit criteria
		- its better to report fewer accurate findings, than many findings with high false positives
		- need to understand the false positives - system flags something as a problem when it isnt a problem
	- vague instructions vs explicit criteria
		- vague - "check that comments are accurate" - claude flags everything that could be inaccurate
		- explicit - "flag comments ONLY when the comments claimed behavior contradicts the actual code" - claude flags only genuine contradictions
	- categorical definitions beat subjective thresholds
		- categorical definition - list of issue types
		- vague - "only report high-confidence findings" - claude decides what exactly is high-confidence
		- explicit - "report only these categories:" null pointer dereference, uncaught async error, skip code style, minor naming, missing documentation - structured description
	- severity criteria with concrete examples
		- when defining severity levels, provide code examples for each level
		- critical - data loss or security vulnerability (writing to DB without transaction rollback)
		- high - functional bug that affects users (returns undefined instead of error object)
		- medium - code quality issue that may cause future bugs (catching exception broadly)
		- low - style issue (dont report these, they are handled by linters)
	- 5 anti-patterns
		- "be conservative" - this is subjective and it lets claude decide - define explicit categories of what to report
		- "only high-confidence findings" - again lets claude decide what is high-confidence - list specific issue types with code examples
		- reporting all possible issues - define what to skip as explicitly what to report
		- keeping high false positives categories - disable the, improve criteria, re-enable later
		- no severity examples - provide code examples for each severity level
	- 5 things to know
		- explicit criteria outperform vague descriptions every time
		- categorical definitions (list of issue types) work better than subjective thresholds 
		- concrete code examples for each severity level
		- define what to skip as explicitly as what to report
		- high false positive rates destroy trust across all categories - disable problematic categories and improve them before re-enabling
part 21
	- domain 4 - task 4.2 - few-shot prompting
		- this is about including 2 to 4 example input and output pairs in your prompt so claude sees the pattern before processing new data
		- why not just instructions - instructions can sometimes fail capture ambiguous edge cases, few-shot examples show claude how to handle things that hard to describe in words
		- useful when instructions alone produce inconsistent results
	- instructions vs examples
		- vague instruction - "check ony my order" - claude guesses get_customer? lookup_order? search_orders? - instructions doesnt clarify
		- few-shot - "check order #1234" -> lookup_order(1234), "check on my order" -> get_customer() first, "orders for john@example.com" -> "get_customer(email="..") - claude learns the patterns
	- force consistent output
		- instructions - "return findings in json with location, issue, severity and suggest fix" - sometimes fields are missing, claude decides how to handle
		- few-shot - "example finding: {"location": "src/auth.ts:45", "issue": "some error" ...}" - claude always returns output that matches this json
	- ambiguous case resolution
		- this is used when correct action is non-obvious, like code-reviews
		- you can teach claude what to flag and what to skip
		- example - acceptable (skip) - if (user?.name) (not a bug, chaining is intentional defensive coding)
		- example - bug (flag) - if (user.name) when user can be null (missing null-check)
		- example - acceptable (skip) - catch (e) { //skip } (comment indicates intentional empty catch, skip it)
	- document extraction
		- when pulling data from documents, examples show how to handle structural differences
		- example 1 - inline citation - "the study (smith et al. 2024) found 94% accuracy" - source: "smith et al. 2024" type: inline_citation
		- example 2 - bibliography reference - "according to reference [3], the error rate decreased.." - source: "Johnson, 2024" type: bibliography_source
		- example 3 - missing field - "revenue decreased by 15% year-over-year" - source: null type: unsourced_claim
		* example 3 teaches claude to return null for missing fields, instead of fabricating a source
	- how many examples to use
		- more examples - more context tokens consumed
		- 2-3 examples optimal for common tasks
		- 4-5 examples optimal for ambiguous tasks
		- 1 example for basic format demonstrations, not enough to teach judgement patterns
		- 0 (zero-shot) - only when task is completely unambiguous
	- 5 anti-patterns
		- not including edge case examples - always include at least one edge case and null example
		- examples without reasoning - always include why each action was choosing
		- too many examples (10+) - wasted tokens, 2-4 is enough
		- examples that all look the same - claude doesnt learn how to differentiate, make examples structurally diverse
		- relying on instructions for format - show exact output format in examples
	- 6 things to know
		- few-shot examples are the most effective technique for consistent output when instructions alone produce inconsistent results
		- include reasoning - "why this action" - so claude learns the pattern
		- show edge cases and ambiguous scenarios - this is where few-shot examples add the most value
		- null and missing field handling - must be demonstrated via examples
		- 2-4 examples is the sweet spot - cover common cases, edge cases, format
		- few-shot examples enable generalization to novel patterns
part 22
	- domain 4 - task 4.3 - structured output patterns (why asking for json output in a prompt is unreliable)
	- why not just ask for json
		- claude decides what structure it should be
		- the solution is tool_usa with json schema
	- the tool_use pattern
		- define an extraction tool - its just a json schema, you tell what field names you want, claude fills in the values
		- force the tool call with tool_choice - guarantee that claude calls your extraction tool instead of returning plain text
		- extract the tool call input - structured data lives in response.content[0].input (matching your valid json schema)
		- this can be used when extracting invoices (you define what fields you want from invoice, claude calls invoice api and uses your tool)
	- guaranteeing structured output
		- tool_choice (auto, any, forced) - never use auto for exaction
	- handling missing information
		- hallucination - fabricated value when a field is missing, you need to tell claude how to handle missing fields
		- you can make the fields nullable in your schema
		- enum with "unclear" and "other" - if document doesnt fit your categories, claude can say so instead of guessing
	- syntax vs semantic errors
		- syntax error isnt possible with tool_use because api enforces valid json at system level
		- semantic errors still can happen (valid json with wrong content)
		- fixing semantic errors are in another step, tool use gives you structure, not correctness
	- 6 things to know
		- tool_use with json schemas eliminates syntax errors (api enforces valid json at system level)
		- tool_choice: "any" - guarantees claude calls a tool instead of returning text
		- nullable fields prevent hallucination - make fields optional
		- enum with "unclear" and "other" prevents forced misclassification of ambiguous cases
		- forced tool selection ensures a specific extract tool runs first when you know the document type
		- tool use eliminates syntax errors but not semantic errors (values of json can still be wrong)
part 23
	- domain 4 - task 4.4 - validation & retry loops
	- why validate after extraction
		- example - json returns invoice with line items, total is 100 but the line item sum doesnt add up - this is a semantic error
	- retry-with-error-feedback
		- when validation fails, send original document plus specific errors back to claude for correction
		- example - extracted = call_extraction_tool(document)
		- example - errors = validate_extraction(extracted)
		- example - if errors: retry_response = client.messages.create(tools, tool_choice, messages)
		- the model sees its own failed attempt AND the specific error, it can self correct because it knows what went wrong
	- when retries wont help
		- retry works - format mismatch (extracted 03/11/2020 instead of 2020-03-11)
		- retry works - structural error (missed line item from invoice)
		- retry wont work - information absent (document missing data)
		- retry wont work - external reference (see document B but document B isnt provided)
	- the detected_pattern field
		- helps improves extraction system over time
		- its like a tag on a warning that tells you what kind of issue was flagged
		- example - {"detected_pattern": "broad-exception-catch"}
	- self-correction validation for numerical data
		- schema design - {"stated_total": {"type": "number"}, "calculated_total": {"type": "number"}, "conflict_detected": {"type": "boolean"}}
		- invoice total is $100, but line items only sum to $92
		- tool flags the conflict_detect for human review
	- feedback loop architecture
		- extract document into structured data
		- validate the result
		- if no errors - accept the result
		- if errors - check if its retryable
		- if not retryable - accept null / flag for review
		- if retryable - retry with feedback and re-validate
	- anti-patterns
		- retrying when data is absent from the source - the correct approach is check if data is not missing, if it is just return null
		- retrying without specific errors - always include previous errors when retrying
		- no detected_pattern field - without this tag, cant analyze false positive trends
		- only checking structural validity - misses the semantic errors
		- unlimited retries - max 1-2 retries
		- not knowing when to retry - retrying when data is missing vs retrying when data was missed or misformatted
	- 5 things to know
		- retry-with-error-feedback - include original document, failed extraction, specific errors in the retry prompt
		- retries only work for structured errors or mismatched format, not for information absence from the source
		- self-correction - extract stated_total and calculated_total, flag conflicts with conflict_detected boolean
		- semantic validation (totals match, dates format consistent) is separate from schema validation (json valid)
	- max 1-2 retries then human review
part 24
	- domain 4 - task 4.5 - batch processing strategies
	- what is message batch api
		- theres 2 ways to call claude
		- synchronous - immedia response, standard cost (chabots, pre-merge checks, perfect for real-time use cases)
		- message batches - up to 24h latency, 50% cheaper cost (overnight reports, weekly audits, for non-blocking use cases)
		* need to know which workflow requires which api
	- critical limitation - no multi-turn tool calling in batch
		- with synchronous - turn 1 (claude calls get_roder() and returns result) -> turn 2 (claude calls check_paymen() and returns result) -> turn 3 (claude gives final answer)
		- with barch - request (analyze this document) -> response (complete analysis in one shot)
		* batch has no tool calls during processing
	- matching the correct api to workflow
		- pre-merge code review - its blocking developers cant wait 24hrs - use synchronous
		- nightly debt report - ready by morning - use batch api
		- weekly security audit - weekend processing - use batch api
	- tracking & correlation - using custom_id to track results in batch api
		- this is like a tracking id
		- each request includes a custom_id that links the respoonse back to the original document
		- batch_requests = {"custom_id": "invoice-01", "params": {...}, "custom_id": "invoice-02", "params": {...}}
		* essential for tracking and identifying failures
	- failure handling - resubmit only what failed
		- idenfiy what failed - loop the batch result, check the status, compile a list with custom id and error message
		- check the error if doc needs modifying (file too long, split the file into chunks) and resubmit the doc
		* "context_limit" error means file is too long and needs to be split into chunks, other errors can be resubmited as is
	- cost optimization - refine prompts before large batches
		- first refine your prompt with synchronous api, check quality, fix prompt issues, continue until quality is acceptable
		- then switch to batch api
	- 5 anti-patterns
		- using batch api for blocking workflows - for real time use synchronous api
		- resubmiting the entire batch on partial failure - resubmit only the failures by using custom_id
		- no prompt refining before large batch - test on a sample set first with synchronous api
		- using batch for multi tool-calling tasks - batch api doesnt support multi tool call, use synchronous for agentic workflows
		- no custom_id on batch requests - without it, you cant track the documents, always include meaningful custom id
	- 6 things to know
		- if someone is waiting for the result - use synchronous
		- always refine prompt before commiting to large batches
		- always include custom_id for batch requests
		- only resubmit only failed batches, not entire batch
		- batching api doesnt support multi tool calling
		- batching api is 50% cheaper but has up to 24h latency
part 25
	- domain 4 - task 4.5 - multi-instance review architectures
		- what catches issues that the code generator is blind to - separate independed instance
		- how to handle inconsistent review quality - multi-pass review architecture
	- why self-review doesnt work
		- claude code is biased on the session where the code was created
		- need to evaluate the code from a fresh session
	- attention problem - single pass fails on large preserving
		- attention dilution - when reviewing a large pull request (14 files) - claude mainly focuses on first and last files
		- inconsistent depth - some files get detailed feedback, others not so detailed
		- contradictory findings - flags pattern as problematic in one file, approves in other files
		- solution - split files into focused passes - per-file local analysis, then cross-file integration and verification
	- multi-pass architecture - three focused passes
		- pass 1 - per-file - each file is reviewed with models full attention in parallel, each file is a new instance 
		- pass 2 - cross-file integration - gather all data from pass 1, pass them into a single instance which focuses on integration issues, data flow, api contract missmatches, missing error propogations
		- pass 3 - verification pass - optional, rates every finding from 1 to 5, findings rated 4+ get auto posted as pull request comments, 2-3 -> human review, 1 -> discarded
	- full pipeline 
		- 14 changed files
		- pass 1 -> 14 instances review each file
		- pass 2 -> 1 instance summarizes findings from pass 1
		- pass 3 -> 1 instance rates findings from pass 2 and posts comments
	- 5 anti-patterns
		- using same session to generate and review code - code is biased, use new session
		- running a single pass for 14+ files - attention delution and inconsistent depth, split into multi-pass
		- using extended thinking as a substitute for code review - still code is biased, use new session
		- low confidence (rating) create too many pull request comments - use verification pass to reduce noice
		- skipping cross-pass entirly - always include it after local passes
	- 5 things to know
		- self-review is biased, use separate session for reviews
		- independed instances per file review catch issues the generator is blind to
		- multi-pass architecture - per-file parallel instances + cross-file integration (sequential) + confidence verification
		- cross-file pass - catches data flow issues, api mismatches, inconsistent patterns
		- confidence rating - routes high-confidence findings to automation, uncertain ones for human reviews
part 26
	- domain 5 - task 5.1 - context management
		- if exam asks about losing details in long conversation - answer is case facts blocks
		- if exam asks about wasting context tokens, the answer is trimmping tool outputs
	- agent forgets context
		- example -  customer is chatting with aget, by turn 15, agent forgets about the previously discussed order
		- this happens because context window fills up with irrelevant data
		- context window is like a desk with stuff on it, when it gets full - you need to remove some stuff
		- models process information in beginning and end more carefully than the middle part
	- progressive summarization risks
		- each prompt gets summarized, but it still can lose critical details (can lose stuff like order id, customer id, custom exact date etc)
		- numbers, dates, identifiers are vulnerable to summarization loss
	- persistent structured data - the "case facts" pattern
		- instead relying on summarization, extract critical facts into strucutred block
		- this structured data is then included in every prompt
		- example - case_facts = {"customer_id": "1234", "order_id": "111", "issues": ["order delayed", "refund requested"]}
	- context budget - trimp verbose tool outputs
		- tool outputs may give too much uneccesary info, trim in PostToolUse hooks before giving response (only return fields that matter)
	- strategic placement - position-aware input organization
		- system prompt - always at the top
		- case facts summary - critical data, near the top
		- detailed tool results
		- latest user message - at the end
	- downstream citation - require metadata in subagent outputs
		- when subagents return findings, require metadata alongside content, so downstream agents can cite sources properly
		- example - {"findings": [{"claim": "some fact", "date": "2020-01-01", "source": "some source"}]}
	- 5 anti-patterns
		- progressive summarization of number and dates - extract a structured case facts block and maintain it seperately
		- including fool tool output with 40+ fields - trim output with PostToolUse hooks of irrelevant data
		- putting all details in middle of context - place summaries of beggining and use session headers
		- subagent output without metadata - require date, source, url in structured output if downstream agents need to cite sources
		- dropping conversation history entirely - pass complete history, trim tool output not the structure
	- 6 things to know
		- lost in the middle means beginning and end processed reliably, middle may be overlooked
		- progressive summarization loses dates, numbers, ids - use case facts blocks
		- trim tool outputs to relevent fields only (40 fields -> 5 fields)
		- position-aware - summaries at top, section headers, latest mesage at end
		- require metadata in subagent outputs for downstream citation
		- complete conversation history for coherence, but trim tool output within that history
part 27
	- domain 5 - task 5.2 - escalation & ambiguity resolution
	- three triggers for escalation
		- trigger 1 - explicit human request - "i want to speak to a manager"
		- trigger 2 - policy gap - "can you match this competitors price"
		- trigger 3 - no progress - multiple approaches tried, non resolved the issue
	- system prompt design - explicit escalation criteria
		- instead of simple prompt "escalate complex cases to human agent" (what is complex?) do explicit few shot examples
		- "escalate when: 1) customer requests a human, 2) refund exceeds $5000, 3) three attempts made with no progress, do not escalate for standard lookups)
		- in other words - explicitly cover when to escalate and when to not escalate with few shot examples
	- exam-critical distinction - sentiment vs case complexity
		- escalate on case complexity, not emotional tone
		- "where is my package?!" - simple lookup, no need escalate
		- "can you refund this product?" - needs management approval, escalate
	- ambiguityresolution - multiple matches - ask, dont guesses
		- when tool output returns multiple results, agent guesses which one to pick
		- the correct approach is to ask for additional identifiers, never guess
	- resolution pattern - acknowledge frustation, then resolved
		- if the problem is resolvable, first offer a resolution, and only escalte if customer reiterates their preference for a human
	- 5 anti-patterns
		- investigating before escalating when a customer asks for a human - escalate immediately on explicit requests
		- sentiment-based escalation, escalating because customer sounds angry - escalate on policy gaps and capability limits
		- guessing when multiple customers match - ask for additional identifiers, nevver guess what to select
		- escalating resolvable issues - resolve autonomasly when within capability
		- vague escalation criteria - define explicit triggers with few-shot examples, show what to escalate and what not to
	- 5 things to know
		- three escalation triggers - ask for human, policy gap, no progress after retries
		- honor human requests - dont investigate, escalate
		- sentiment is unreliable - escalate based on case complexity, not emotional tone
		- multiple matches need clarification - ask for more identifiers
		- explicit criteria with examples product consistent escalation behavior
part - 28
	- domain 5 - task 5.3 - error propagation in multi agents
		- this is all about creating a system that adapts to errors intelligently
		- single agent systems dont have this problem because when tool fails, you handle it
		- multi agent systems need to handle error reporting via coordinator for recovery decisions
	- building blocks - why error quality matters
		- bad protocol - "there was a problem"  - tells nothing
		- good protocol - "customer X have requested order Y to be refunded" - has details
	- structured error content - what a good error report looks like
		- subagent returns this to coordinator on failure
		- {"status": "partial_failure", "failure_type": "timeout", "partial_results": [...], "what_was_missed": "could not reach something", "alternative approaches": [...]}
		- coordinator now needs to decide - retry, use partical results, or note the gap
		- without detailed error coordinator would just see "search failed"
	- generic vs structured errors - the difference is actionability
		- generic - doesnt know partial results exit, doesnt know alternatives
		- structured - has partial results, alternatives, full context for intelligent recovery
	- correct approach - graceful degradation, adapt and continue
		- coordinators job is to proceed with partial results and anotate the missing gaps, dont abort the entire workflow
	- coverage annotations - annotate whats reliable and what isnt
		- when a subagent partially fails, the final result needs to be annotated which results are well suported, and which have gaps
	- local recovery - handle transient errors locally before propagating
		- before reporting an error to coordinator, try fixing the issue locally first
		- when failing fixing the issue locally, include how many attempts were tried in the error report
	- 7 anti-patterns
		- silent suppression, returning empty response instead of error - distinguish failures from valid empty results, return a structured error with failure type
		- immediate termination when one sub agent fails - continue with parial results from successfull agents, note which areas failed, partial output better than no output
		- generic "unavailable" errors - always include structured context like failure type, partial results, alternatives
		- retuirning empty result as seccuss when an error happened - distinguish valid empty results from errors
		- terminating workflow on first subagent failure - continue with partial results, annotate what failed
		- no coverage annotations - always annotate well-supported vs partial vs gap areas
		- propagating every transient error - handle transient errors locally with retries, dont clutter coordinator context if possible
	- 5 things to know
		- when a subagent fails - structured error must include failure type, attempted query, partial results, alternative approach
		- never silently suppress errors - dont return empty result as a success when an error happened
		- never terminate on first failure - dont stop entire workflow, use partial results from other subagents and annotate whas failed
		- coverage annotations in synthesis output (well-supported, partial, gap)
		- local recovery in subagents for transient errors
part 29
	- domain 5 - task 5.4 - large codebase context (investigation)
		- if exam asks how to keep context clean during code exploration - subagent delegation with scratchpad files
		- if it tasks about recovery - state persistance
	- the problem - content degradation in large codebases
		- when starting a session, in large code bases, claude would give accurate responses while context is still fresh
		- over time, the context degrades and you will get vague references instead of specific details
		- this topic is about detecting and preventing context degradataion
	- solution 1 - scratchpad files, persistent external memory
		- writing summaries of findings instead of keeping everything in memmory
		- when context window fills up, agent reads these summaries instead of analyzing the code again
	- solution 2 - subagent delegation for exploration
		- instead of doing all exploration in the main agent, delegate the exploration for subagents and summaries their findings
		- this makes the main agent context window with jest few lines instead of multiple source files
	- solution 3 - /compact command
		- compresses hours of discussions
		- this is a tool to summarize your conversation, use when noticing conversations becoming vague
	- solution 4 - structured state persistence for crash recovery
		- for multi-hour investigations, agents should export their findings to a known location so work survives a session crash
		- on crash, coordinator then loads the manifest, injects it into the new sessions context, and continues where it let off
	- solution 5 - summarize between phases
		- when investigation has multiple phases, summarize each phase and spawn a new agent for next phase, pass the summary from previous phase downstream
	- 5 anti-patterns
		- reading all files in main agent - delegate to subagents, summarize the findings, main agent should make the decisions, not read source code
		- no scratchpad for extended investigation - write findings to a file, reference later, this preserves discoveries across context bondries
		- ignoring context degradation signals - use /compact proactively, deleage to subagents
		- no state persistance - export structured state to manifest files so work can be recovered
		- passing raw phase 1 output to phase 2 - summarize phase finding before injecing them to next phases
	-  6 things to know
		- context degrades over time, recognise the signal (no specific details)
		- scrachpad files provide persistent memory, write key findings to files to reference them later
		- delegate subagents to explore to keep the main agent context clean
		- /compact command compresses session conversation, use proactively
		- state persistence enables crash recovery, export manifest files
		- summarize between phases, inject summary of phase 1 to phase 2 and so on
part 30
	- domain 5 - task 5.5 - human review workflows (field extraction from documents)
		- no ai system is 100% accurate, the goal is to only route cases that actually need human review
		- confidence callibration determines if issue needs to be resolved locally or handled for human review
		- if exam asks about accuracy metrics - always validate by document type and field
		- if exam asks about confidence scores - always calibrate against labeled validation sets before setting up automation
		- if exam asks about quality monitoring - use stratified sampling to catch difficult document types
		- if exam asks about review workflow - route by confidence: auto-accept high, human review medium, flag low as failed
		- if exam asks about systemn improvement - it means human review must be fed back into the loop (recalibration, prompt changes)
	- field-level confidence scores
		- vendor_name: {value: "some vendor", confidence: 0.98}
		- total_amount {value: $1234, confidence: 0.92}
		- tax_amount: {value: null, confidence 0.6}
		- category: {value: "unclear", confidence: 0.3}
		- >= 0.95 - auto accept
		- 0.5 - 0.95 - flag for human review, uncertain but extractable
		- > 0.5 - flag as unable to extract
	- calibration - model confidence is not actual accuracy
		- step 1 - create a labeled validation set of 100+ documents
		- step 2 - run the extraction model on all of them
		- step 3 - compare model confidence to actual accuracy
		- step 4 - adjust thresholds
	- ongoing monitoring - stratified random sampling
		- u have 100 documents, 90 of one type and 10 of other type, random sampling would most likely take always documents from the 90 pool
		- stratified sampling means taking samples from rare and difficult categories  
	- full workflow - from extraction to feedback loop
		- model extract fields with per field confidence scores
		- routing - high -> auto-accept, medium -> human review, low -> flag as failed
		- human review - human sees document, extraction, and flagged fields with scores
		- human correction feeds back into three systems
		- 1) the final output
		- 2) calibration data to update threshold
		- 3) error pattern analysis to improve extraction prompts over time
	- 5 anti-patterns
		- relying on aggregate metrics - validate accuracy by document type and field seperately
		- trusting model confidence without calibration - calibrate thresholds with labeled validation sets
		- random sampling without stratification - use sampling weighted towards hard or rare types
		- automating all hiigh-confidence extractions immediately - validate accuracy before automating
		- no feedback loop from human corrections - use human corrections to update calibration confidence and improve extraction prompts over time
	- 5 things to know
		- field-level confidence - each extraction is scored individually, not the whole document
		- calibrate thresholds - model can be innacurate, need label validation sets
		- stratified sampling - sample rare/difficult document types, dont pick randomly
		- route by confidence - auto accept high confidence, human review for medium, flag low as failed
		- feedback loops - human corrections update calibration and improve prompts over time
part 31 
	- domain 5 - task 5.6 - provenance & uncertainty (report, news, statistics)
		- if exam asks can a human verify every claim in the final report - if not, provenance was lost somewhere in pipeline
		- if exam asks what preserves attribution - structured claim-source mappings
		- if exam asks how to handle conflicts - annotate both values with context
		- if exam asks about dates - its about including publication date in source metada and showing it in final report
		- if exam asks about output structure - well-established, contested, gaps
		- if exam asks about rendering - dont render all reports in one format
	- the problem - attribution loss during compression
		- each summarization strips away sorce details
	- the solution - structured claim0source mappings
		- every subagent pairts each clain with metadata, this metadata always persists
	- conflict of sources - annotate the conflict, dont pick a winner
		- two or more credible sources report different numbers, main agent should present both with context, dont pick just one
	- temportal data - publication dates
		- important to preserve source publication dates, final reports without dates will be more accurate
	- synthesis output - strucure by confidence level
		- final report should distinguish by ho well-supported each claim is
		- reader needs to know what is accurate, what is contested, and what has gaps
	- 5 anti-patterns
		- summarizing findings without claim source mappings - each subagent should gather and preserve metadata containing url, name, date, excerpt, methodology
		- choosing one source when multiple sources conflict - annotate conflicts with both values and source context
		- omitting publication dates - always require publication date in structured output
		- forcing every content type into the same format - each content type is rendered in its natural format - tables for financial data, prose for news articles, list for technical specs
		- failing to distinguish well-supported and contested claims - structured output by confidence level - well-established, contested findings, gaps
	- 5 things to know
		- claim-source mappings must survive every level of synthasis
		- conflicting sources - annotate all sources, dont pick one
		- publication dates turn apparent contradictions into trends, always require them in subagent outputs
		- synthesis output must distinguish well-established, contested, gap findings
		- content-type rendering - financial data as tables, news as prose, technical data as lists, dont output everything in one format